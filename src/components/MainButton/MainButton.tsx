interface MainButtonProps {
    link?: string,
    action?: () => {}
}

const MainButton = ({link, action} : MainButtonProps) => {
    return (  
        <>
        </>
    );
}

export default MainButton;